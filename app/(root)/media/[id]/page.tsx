import React from "react";

type MediaPageProps = {
  params: {
    id: string;
  };
};

const MediaPage = ({ params: { id } }: MediaPageProps) => {
  return <div>MediaPage</div>;
};

export default MediaPage;
